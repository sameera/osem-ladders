# Application Configuration
# Display name shown throughout the application
VITE_BRANDING_APP_NAME=Career Ladder Assessment

# AWS Cognito OIDC Configuration
# Replace these values with your actual AWS Cognito configuration
# This app uses standard OIDC (react-oidc-context) instead of AWS Amplify

# AWS Region where your Cognito User Pool is located (e.g., us-east-1, us-west-2)
VITE_COGNITO_REGION=us-east-1

# Your Cognito User Pool ID (found in AWS Console)
# Format: region_XXXXXXXXX
VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX

# Your Cognito User Pool Client ID (App Client ID)
# Found in App Integration > App clients section of your User Pool
VITE_COGNITO_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Your Cognito Domain (without https://)
# Format: your-domain.auth.region.amazoncognito.com
# Found in App Integration > Domain name section of your User Pool
VITE_COGNITO_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com
